import {StyleSheet} from 'react-native';
import {Colors, FontStyles} from '@themes';

const Styles = StyleSheet.create({
  title: {
    ...FontStyles.textNormal,
    color: Colors.white,
  },
});

export default Styles;
